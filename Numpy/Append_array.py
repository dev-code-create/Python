import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

c = a + b
print(c)

arr = np.array([[1, 2], [3, 4]])

print(arr.shape)   # rows, columns
print(arr.mean())  # average
print(arr.max())

